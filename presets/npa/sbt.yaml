name: SBT
module: npa
description: "PLACEHOLDER — fill table names once mapping is confirmed."

cases:
  - name: placeholder
    rule_path: PLACEHOLDER_npa_sbt
    tables:
      - PLACEHOLDER_TABLE_1
