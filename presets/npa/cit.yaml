name: CIT
module: npa
description: "PLACEHOLDER — fill table names once mapping is confirmed."

cases:
  - name: placeholder
    rule_path: PLACEHOLDER_npa_cit
    tables:
      - PLACEHOLDER_TABLE_1
