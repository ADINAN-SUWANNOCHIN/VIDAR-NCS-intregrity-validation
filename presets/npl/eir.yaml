name: EIR
module: npl
description: "PLACEHOLDER — fill table names once mapping is confirmed."

cases:
  - name: placeholder
    rule_path: PLACEHOLDER_npl_eir
    tables:
      - PLACEHOLDER_TABLE_1
